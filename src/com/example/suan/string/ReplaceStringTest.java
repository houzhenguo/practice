package com.example.suan.string;

public class ReplaceStringTest {

    public static void main(String[] args) {

    }

    // 替换字符串 比如 caabbc ab -> 替换之后为 cc 直到所有的替换完成
    public static String replace(String supStr, String rStr) {
        char[] s1 = supStr.toCharArray();
        char[] temp = new char[s1.length];
        for (int i = 0; i < s1.length;i++) {
            // 不想做了
        }
        return "";
    }
}
